# Module Pipeline - Dependencias de Producción
# Proyecto: La Maquina de Noticias
# Versión optimizada sin dependencias de desarrollo
# Reducción esperada: ~200MB

# === FRAMEWORKS WEB ===
fastapi==0.115.12                   # Framework web async de alto rendimiento
uvicorn[standard]==0.30.6           # Servidor ASGI para FastAPI

# === IA/ML ===
groq==0.26.0                        # Cliente API para LLMs Groq
spacy==3.8.2                        # NLP framework (sin modelos, se descargan aparte)
# NOTA: sentence-transformers removido - no se usa y añade PyTorch/CUDA (~2GB)

# === DETECCIÓN DE IDIOMA ===
langdetect==1.0.9                   # Detección de idioma (mantener solo uno)
# langid==1.1.6                     # REMOVIDO: duplicado con langdetect

# === BASE DE DATOS ===
supabase==2.4.6                     # Cliente Supabase
psycopg2-binary==2.9.10             # Driver PostgreSQL

# === VALIDACIÓN Y CONFIGURACIÓN ===
pydantic==2.11.5                    # Validación de datos
pydantic-settings==2.3.0            # Configuración basada en Pydantic V2
python-dotenv==1.1.0                # Carga de variables de entorno

# === HTTP CLIENTS ===
httpx==0.27.2                       # Cliente HTTP async moderno
aiohttp==3.9.5                      # Cliente HTTP async (usado por algunas dependencias)

# === UTILIDADES CORE ===
tenacity==9.1.2                     # Reintentos con backoff exponencial
loguru==0.7.3                       # Logging estructurado
python-dateutil==2.9.0              # Manipulación de fechas

# === PROCESAMIENTO ===
numpy>=1.21.0,<2.0.0                # Arrays numéricos (requerido por spaCy)

# === INFRAESTRUCTURA ===
nest-asyncio==1.6.0                 # Compatibilidad asyncio anidado

# === PARSEO Y REPARACIÓN ===
json-repair==0.27.1                 # Reparar JSON malformado de LLMs

# === TOKENIZACIÓN ===
# tiktoken==0.8.0                   # REMOVIDO: tokenización OpenAI no usada

# === MONITOREO (Opcional pero recomendado) ===
sentry-sdk==2.17.0                  # Monitoreo de errores en producción

# === NOTAS DE OPTIMIZACIÓN ===
# - Removidas dependencias de testing: pytest, pytest-mock, pytest-asyncio (~50MB)
# - Removido sentence-transformers: no se usa, instala PyTorch/CUDA (~2GB)
# - Removido langid: duplicado con langdetect (~10MB)
# - Removido tiktoken: tokenización OpenAI no usada (~20MB)
# - Total reducción esperada: ~200MB en dependencias
#
# === INSTALACIÓN DE MODELOS SPACY ===
# Los modelos de spaCy se instalan por separado en el Dockerfile:
#   python -m spacy download es_core_news_md  # Versión mediana (47MB) en lugar de lg (568MB)
#   python -m spacy download en_core_web_sm   # Versión pequeña (13MB)
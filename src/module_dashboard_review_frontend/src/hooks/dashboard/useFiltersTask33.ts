// Hook de filtros para Task 33
// Re-exporta el hook principal con alias de tipos para compatibilidad

export { useFilters } from './useFilters';
export type { FilterState as FilterParams } from '@/types/domain';
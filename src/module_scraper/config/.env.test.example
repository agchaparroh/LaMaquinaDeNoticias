
# Supabase Configuration

SUPABASE_PROJECT_NAME=""
SUPABASE_PROJECT_ID=""
SUPABASE_URL=""
SUPABASE_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_DB_PASSWORD=""

# Para las migraciones PostgreSQL directas
PGHOST= ""
PGPORT= ""
PGDATABASE= ""
PGUSER= ""
PGPASSWORD= ""

# Test-specific settings
LOG_LEVEL="DEBUG"
SUPABASE_STORAGE_BUCKET=""

# Playwright Error Handling Configuration
# Maximum number of retries for Playwright-specific errors (default: 2)
PLAYWRIGHT_MAX_RETRIES="2"

# Timeout for Playwright operations in milliseconds (default: 30000 = 30 seconds)
PLAYWRIGHT_TIMEOUT="30000"

# Enable fallback to non-Playwright requests when Playwright fails (default: True)
PLAYWRIGHT_ENABLE_FALLBACK="True"

# Maximum number of retries for empty content detection (default: 1)
PLAYWRIGHT_MAX_EMPTY_RETRIES="1"

# Enable Playwright for empty content detection (default: True)
USE_PLAYWRIGHT_FOR_EMPTY_CONTENT="True"
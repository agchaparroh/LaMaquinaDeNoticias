import '@testing-library/jest-dom/vitest'

// Global test setup for Vitest
// This file extends the expect matchers with jest-dom matchers
// like toBeInTheDocument(), toHaveValue(), toBeVisible(), etc.